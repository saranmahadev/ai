---
import TopicLayout from "../../../layouts/TopicLayout.astro";
import { STAGES } from "../../../data/roadmap";

const stage = STAGES.find((s) => s.slug === "ml-fundamentals")!;
---

<TopicLayout stage={stage} />
